[pytest]
env_override_existing_values = 1
env_files = .env
pythonpath=src
filterwarnings =
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning:openpyxl\.
markers =
    integration: marks integration tests (use -m "integration" to run)